

function Tasks() {
    const [currentTasks, setCurrentTasks] = useState(["Procrastinate", "Push things off", "Wait til tomorrow"]);

    return (
        <Button  />
        <Button />
        <Button />
    );
}